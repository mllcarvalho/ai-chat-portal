# BMAD Product Studio — Extensão VS Code

Proxy local que expõe os modelos e MCPs do GitHub Copilot via HTTP (127.0.0.1) para a interface web do BMAD Product Studio. Instalada automaticamente pelo `npm start` na raiz do repositório.
